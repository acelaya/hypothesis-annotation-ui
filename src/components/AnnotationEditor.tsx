import { Annotation, Draft } from '../helpers/types';

export type AnnotationEditorProps = {
  /** The annotation under edit */
  annotation: Annotation;
  /** The annotation's draft */
  draft: Draft;
};

export default function AnnotationEditor(props: AnnotationEditorProps) {
  return null;
}
